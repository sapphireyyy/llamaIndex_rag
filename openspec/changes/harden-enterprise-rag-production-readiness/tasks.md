## 1. 基线与验收夹具

- [x] 1.1 为上传接口增加契约测试：慢解析、慢 Embedding 和慢索引不得阻塞 `202`，响应只包含持久化的 queued 任务。
- [x] 1.2 为任务处理增加故障夹具，覆盖重复投递、Worker 中途退出、租约过期、取消、重试上限和双索引发布失败。
- [x] 1.3 建立环境驱动的 PostgreSQL RLS 测试夹具，分别接收迁移连接与非特权运行连接，不硬编码主机、端口或凭证。
- [x] 1.4 建立可控模型 Provider 夹具，能够模拟延迟真实 delta、仅完整回答、首 delta 前失败、首 delta 后失败、取消和最终 usage。
- [x] 1.5 添加合法/非法切分配置、配置切换中任务和相同内容重新处理的测试数据。
- [x] 1.6 添加含原生文本页、空白扫描页、低置信度页和失败页的无敏感信息 OCR 测试文档。
- [x] 1.7 添加陈旧 ACL、旧文档版本、旧处理代次和孤立外部索引候选的检索测试夹具。
- [x] 1.8 记录当前测试、静态检查、迁移、Compose 配置和关键 smoke 基线，明确哪些新增契约在实现前预期失败。

## 2. P0 数据模型与幂等处理基础

- [x] 2.1 新增向前兼容迁移，为入库任务增加认领者、租约到期、心跳、下次重试时间和条件状态转换所需索引。
- [x] 2.2 新增文档索引处理代次模型及活动代次指针，关联内容版本、租户配置版本、策略哈希、组件版本和发布状态。
- [x] 2.3 为 Chunk 与检索投影增加 `index_generation_id`，并让唯一约束支持同一内容版本的多个处理代次。
- [x] 2.4 为现有活动文档回填兼容的基线处理代次和活动指针，确保迁移后查询结果不变。
- [x] 2.5 更新 ORM、领域类型和仓储边界以表达任务租约、处理代次、活动切换和最后有效代次回滚。
- [x] 2.6 将 Chunk、暂存投影、配额消费、审计与终态写入改为基于任务/处理代次的确定性防重效果。
- [x] 2.7 添加迁移 upgrade/downgrade、存量回填、唯一约束和旧代码兼容测试。

## 3. P0 队列确认与 Worker 消费链

- [x] 3.1 将消息队列端口改为显式 delivery 生命周期，只有业务处理提交后才能 `ack`，失败或取消时可 `nack`/重排。
- [x] 3.2 更新进程内队列适配器以实现与生产队列一致的 delivery、重试和测试可观测语义。
- [ ] 3.3 修正 RabbitMQ 适配器，保留消息处理上下文直到 handler 完成，并验证 publisher confirm、ACK、NACK、重连和预取行为。
- [x] 3.4 为 Outbox Dispatcher 增加并发安全的行级认领与 `SKIP LOCKED`，保持退避、毒消息和幂等发布。
- [x] 3.5 实现租户感知的消息路由，只接受已知任务种类，并校验消息租户与 PostgreSQL 任务租户一致。
- [x] 3.6 实现任务租约服务和条件状态转换，支持认领、续租、过期恢复、完成、失败和取消竞争。
- [x] 3.7 将自动重试改为持久化调度，区分 transient/content 错误并在重试上限后进入可诊断终态。
- [x] 3.8 在解析、OCR、Embedding、每个索引阶段及活动切换前增加租约与取消检查，确保取消不发布新代次。
- [x] 3.9 在 Worker 入口以结构化并发运行 Outbox 分发、队列消费、租约恢复和优雅停止，停止时不提前 ACK 未提交任务。
- [ ] 3.10 增加 Worker 单元与 RabbitMQ 集成测试，证明重复消息、并发消费者和重启最终收敛为同一业务结果。

## 4. P0 上传 API 真异步切换

- [x] 4.1 在上传提交事务中同时创建/复用文档版本、queued 任务、幂等记录和 `ingestion.process` Outbox 事件。
- [x] 4.2 从上传 API 请求路径移除 `process_with_retries()`，在数据库事务成功提交后立即返回 `202` 和初始任务视图。
- [x] 4.3 保持相同幂等键与请求哈希返回同一逻辑任务，并拒绝相同幂等键对应不同内容。
- [x] 4.4 增加入库执行模式配置；production 必须使用 queued + durable queue，inline/inprocess 仅允许显式开发或测试场景。
- [x] 4.5 为数据库提交失败后未被版本引用的确定性原始对象实现保留期与 orphan 清理任务。
- [x] 4.6 更新前端上传流程，在收到 queued 任务后立即展示并轮询/刷新阶段状态，而不是假定上传响应已经完成入库。
- [ ] 4.7 增加 API 进程响应后退出、队列暂时不可用、Outbox 恢复、Worker 重启、重复提交和取消的端到端测试。
- [ ] 4.8 在 Compose 拓扑中验证 API 不执行入库重活、Worker 实际消费任务，并验证多 Worker 并发下的背压与任务收敛。

## 5. P0 PostgreSQL RLS 发布门禁

- [x] 5.1 分离迁移数据库 URL 与应用数据库 URL，使用一次性迁移步骤执行 DDL，并确保 API/Worker 运行环境不持有迁移凭证。
- [x] 5.2 加固运行角色初始化 SQL，显式设置 `NOSUPERUSER NOBYPASSRLS NOINHERIT`、最小 DML 权限、默认权限和非 owner 关系。
- [x] 5.3 实现只读 RLS 预检，核对当前角色属性、角色成员关系、受保护表 owner、RLS/force 标志和策略 `USING`/`WITH CHECK` 覆盖率。
- [x] 5.4 让受保护表清单由迁移与验证共享，新增租户表时若未加入 RLS 覆盖即使测试失败。
- [ ] 5.5 扩展真实 PostgreSQL 契约测试，覆盖每类租户表的跨租户 SELECT、直接主键读取、INSERT、UPDATE 和 DELETE。
- [ ] 5.6 增加无租户上下文、无效租户上下文、连接池归还/复用和事务异常后的 fail-closed 测试。
- [ ] 5.7 将非特权运行账号的 RLS 套件接入生产化 smoke 与发布门禁，特权、bypass 或 owner 账号必须明确失败。
- [x] 5.8 更新部署模板与运维说明，只记录 Secret 引用和账号职责，不输出实际数据库凭证。

## 6. P1 租户级处理配置与索引代次

- [x] 6.1 演进租户配置 schema，增加版本化 `ingestion` 配置组及 `chunk_size`、`chunk_overlap`、OCR 模式、阈值、Provider 和资源上限校验。
- [x] 6.2 实现旧配置兼容解析器，用有标识、有版本和稳定哈希的默认处理策略保持现有 `512/64` 与 OCR disabled 行为。
- [x] 6.3 在配置创建、验证、激活、回滚和 API 输出中保留处理策略版本与字段级错误，继续禁止原始 Secret。
- [x] 6.4 更新租户管理界面以编辑切分和 OCR 策略，显示继承值、验证错误、活动配置版本和变更影响说明。
- [x] 6.5 在任务提交时解析并持久化完整处理快照，Worker 只使用快照而不读取可能已变化的当前配置。
- [x] 6.6 将切分器构造、Chunk provenance 与索引 metadata 改为使用任务的有效配置版本、参数和处理代次。
- [x] 6.7 实现处理代次的暂存、数量/ACL/版本校验、外部投影准备、PostgreSQL 指针原子切换和失败回滚。
- [x] 6.8 提供按文档、数据源和知识空间提交的显式重建任务，复用普通队列、配额、并发、取消和审计语义。
- [x] 6.9 更新管理界面以提交重建、跟踪任务、查看处理配置/代次并在授权下回切最后有效代次。
- [x] 6.10 实现旧处理代次的保留期清理和孤立投影清理，确保活动代次与审计引用永不被提前删除。
- [x] 6.11 添加无效参数、任务期间配置切换、相同内容重新切分、原子发布失败和代次回滚测试。

## 7. P1 受策略约束的 OCR

- [x] 7.1 定义项目自有 OCR Adapter、能力描述和结果类型，包含文本区域、页码、坐标、置信度、语言、Provider 与版本。
- [x] 7.2 扩展结构解析输出，保留无原生文本或低文本页面的诊断及可供 OCR 的页面渲染输入，而不是静默丢弃扫描页。
- [x] 7.3 在可选 `ocr` 依赖组中实现并固定首个本地 OCR Adapter，同时提供确定性 fake/no-op Adapter 供单元测试与 disabled 模式使用。
- [x] 7.4 将 OCR Adapter 接入 Provider 注册与能力校验，对远程能力统一执行租户外联、Secret Binding、内容捕获和允许列表检查。
- [x] 7.5 在入库编排中实现 `disabled`、`best_effort`、`required` 三种模式，以及页面阈值、超时、页数、并发和取消限制。
- [x] 7.6 将 OCR 来源、区域、置信度、Provider 与处理版本写入 ParsedUnit、Chunk、处理代次和引用定位信息。
- [x] 7.7 记录 OCR 页数、成功/失败、降级、耗时、Provider 用量与成本，并按租户配额阻止失控处理。
- [ ] 7.8 使用扫描型中文 PDF 验证原始页面引用、外联禁止、低置信度、部分失败、required 失败和最后有效代次保留。

## 8. P1 完整检索策略与生产 Provider

- [x] 8.1 定义并持久化完整检索策略 schema，覆盖双路 top-k、fusion k、rerank top-k、evidence top-k、阈值、上下文预算和单路降级。
- [x] 8.2 为现有租户创建有版本和哈希的兼容默认检索策略，移除查询对未追踪 dataclass 默认值的依赖。
- [x] 8.3 实现租户感知的有效检索策略解析器，支持版本化继承并拒绝缺失、未激活、跨租户或能力不兼容的引用。
- [x] 8.4 在助手版本验证/激活时校验完整检索策略和 Provider Profile，并在查询开始后冻结不可变快照。
- [x] 8.5 让所有召回、融合、重排、证据选择、阈值和预算读取有效快照，并在查询记录/trace 中保存 ID、版本、哈希、继承来源和最终值。
- [x] 8.6 实现基于助手版本的模型 Provider 解析，验证租户归属、状态、Secret Binding、外联策略和能力，而不是始终使用全局容器模型。
- [x] 8.7 加强 production 配置校验：必须显式配置生成式 Provider；`extractive` 只允许开发、测试或活动策略明确指定的降级路径。
- [x] 8.8 实现受策略控制的 Provider 回退，在审计、回答状态和用量中同时记录主 Provider 失败与实际回退，不允许静默降级。
- [x] 8.9 更新助手配置界面以展示有效 Provider/检索策略、能力验证、版本来源和 production 不兼容原因。
- [ ] 8.10 添加自定义策略、版本化默认值、查询中途激活新版本、跨租户引用、无效 Secret、生产 extractive 和显式回退测试。

## 9. P1 真实流式回答与唯一终态

- [x] 9.1 定义模型增量事件和精确能力声明；移除 `extractive` 及完整回答适配器对真实 streaming 能力的误报。
- [x] 9.2 实现 OpenAI-compatible 上游流式协议解析，直接产出 delta、finish reason 和最终 usage，并正确关闭连接。
- [x] 9.3 更新 ModelGateway，使重试/回退只发生在首 delta 前；首 delta 后失败不得重放或拼接第二个 Provider 的答案。
- [x] 9.4 实现跨 delta 窗口的增量敏感信息护栏，并让需要完整答案判断的策略显式选择 `buffered`。
- [x] 9.5 将查询执行重构为事件驱动入口，在检索后按 Provider 能力产生 streaming/buffered 事件并只在最终校验后持久化成功答案和引用。
- [x] 9.6 保持现有 SSE 事件名，在首个 `query` 事件加入 `delivery_mode` 和能力快照标识，并删除完整答案后的固定长度伪分片。
- [x] 9.7 使用条件状态转换保证每个查询只有一个终态，断连与取消 API 必须向下取消 Provider 流并停止后续事件。
- [x] 9.8 更新前端流处理与界面状态，兼容新增模式字段、buffered 单次回答、中途失败、取消和唯一 terminal。
- [ ] 9.9 增加首 delta 延迟、顺序、Unicode/SSE 边界、usage、引用、首 delta 前后失败、取消、断连和拆分敏感模式测试。

## 10. P1 PostgreSQL 事实源与检索投影对账

- [x] 10.1 统一 dense/lexical 投影 metadata，包含租户、空间、文档、内容版本、处理代次、ACL tokens/epoch 和 published 标志。
- [x] 10.2 在所有 SearchAdapter 中下推等价的租户、空间、ACL、活动代次、发布和可选文档过滤，并补齐适配器契约测试。
- [x] 10.3 在模型上下文组装前按候选批量查询 PostgreSQL，重新验证当前权限、ACL epoch、文档状态、活动内容版本和活动处理代次。
- [x] 10.4 复用权威校验处理引用源访问，权限撤销、文档删除或版本/代次切换后旧引用必须失效。
- [x] 10.5 实现陈旧候选指标和可审计诊断，按索引、租户、文档与失效原因分类且不记录受保护正文。
- [x] 10.6 实现从 PostgreSQL 与受管原始内容出发的索引对账/修复任务，比较数量和关键 metadata 后补写或重建投影。
- [ ] 10.7 验证 PostgreSQL 不可用时 fail-closed，以及单个外部索引不可用时只按版本化策略进行单路降级。
- [ ] 10.8 添加陈旧授权、旧版本、旧处理代次、孤立投影、对账修复、PostgreSQL 中断和撤权引用测试。

## 11. 可观测性、运维与兼容文档

- [x] 11.1 增加 Outbox/队列/ACK、Worker 租约、任务阶段、重复收敛和毒消息指标，并贯通 correlation/job ID。
- [x] 11.2 增加 delivery mode、首 delta/总延迟、流式失败/取消、实际 Provider/回退、策略哈希和 usage 指标。
- [x] 11.3 增加 OCR、处理代次、重建进度、陈旧索引和对账结果指标，应用租户内容捕获与保留策略。
- [x] 11.4 更新仪表盘和告警，覆盖 Outbox 堵塞、队列积压、无活跃 Worker、租约恢复、RLS 门禁失败、流式失败和索引陈旧。
- [x] 11.5 编写异步入库、队列恢复、毒消息、代次回滚、OCR 故障、Provider 降级、索引对账和 RLS 失败的运维手册。
- [x] 11.6 更新部署与配置文档，说明迁移/运行账号、production Provider 要求、开发兼容默认值、切分/OCR 策略和迁移开关退场条件。
- [x] 11.7 更新用户操作指南，覆盖 queued 上传进度、重建、处理代次、配置验证、真实流式/buffered 状态与可诊断失败。

## 12. 全量验证与发布证据

- [x] 12.1 运行并修复后端单元/集成/契约测试、前端测试、Ruff、MyPy、TypeScript、ESLint 和生产构建。
- [ ] 12.2 在 SQLite 开发路径和真实 PostgreSQL 上验证增量迁移、存量回填、向后兼容读取、downgrade 与活动代次回滚。
- [ ] 12.3 验证 Compose 配置与镜像构建，延迟检查 API、Worker、Web、PostgreSQL、RabbitMQ、Milvus 和 OpenSearch 的真实状态。
- [ ] 12.4 执行 P0 smoke：上传快速返回 queued、API 响应后退出、Worker 完成任务、重复投递收敛、取消和发布失败保留旧版本。
- [ ] 12.5 执行非特权非 owner PostgreSQL RLS smoke，证明无上下文和跨租户读写被拒绝且连接池不泄漏租户状态。
- [ ] 12.6 执行 P1 smoke：版本化切分与重建、扫描 PDF OCR、完整检索策略、显式生成式 Provider、真实 delta、buffered 和取消。
- [ ] 12.7 注入 PostgreSQL、RabbitMQ、Worker、OCR、模型、Milvus 和 OpenSearch 故障，验证 fail-closed、受控降级、恢复与无越权数据泄露。
- [ ] 12.8 建立上传响应时间、队列吞吐/积压恢复、首 delta、查询总时延、OCR 吞吐与索引重建的基线和可接受阈值。
- [x] 12.9 生成不含 Secret 的发布证据包，逐项映射 spec 场景、测试结果、运行配置版本、迁移状态、已知限制和回滚证据。
- [ ] 12.10 运行 `openspec validate harden-enterprise-rag-production-readiness --strict` 并确认所有任务完成后再进入归档流程。
