# Enterprise RAG 页面操作指南

本文面向使用 Enterprise RAG 知识助手页面的管理员、编辑者和普通用户，说明页面各入口的用途、标准操作流程、状态含义以及常见问题。

## 1. 进入页面前

### 1.1 启动服务

在项目根目录分别启动 API 和前端：

```powershell
# API
uv run uvicorn enterprise_rag.main:app --app-dir backend --reload

# 前端（Windows）
npm.cmd --prefix web run dev
```

浏览器打开 <http://localhost:3000>。页面左下角的系统状态会显示 API 是否已就绪：

- **系统就绪**：API、数据库以及页面依赖已通过就绪检查。
- **检查中/降级**：页面可能仍可打开，但部分问答、上传或评测功能不可用；先检查 API 终端输出和 `/health/ready` 返回的 `configuration_errors`。

### 1.2 权限角色

页面中的操作受当前用户和知识空间成员关系控制：

| 角色 | 主要权限 |
| --- | --- |
| 管理员 | 管理知识空间、成员、文档、助手版本、质量门禁 |
| 编辑者 | 管理授权范围内的知识内容，并参与助手配置相关操作 |
| 读取者 | 使用已授权的助手进行问答，查看自己有权限的引用来源 |

系统按租户隔离数据。没有匹配的成员关系或授权策略时，默认没有访问权限。

## 2. 页面导航

左侧导航包含四个入口：

1. **问答**：选择助手并提出问题，查看流式回答和引用来源。
2. **知识**：维护知识空间、成员、上传文档和摄取任务。
3. **配置**：创建助手、生成配置草稿、校验并激活版本。
4. **质量**：创建评测集、运行候选版本、查看指标和质量门禁。

建议首次使用时按“知识 → 配置 → 问答”的顺序操作；需要上线前验证时再进入“质量”。

## 3. 知识：准备可检索内容

### 3.1 创建知识空间

1. 进入左侧的 **知识**。
2. 在左侧“知识空间”区域填写空间名称和用途说明。
3. 点击 **新建空间**。
4. 在列表中点击新空间，使其成为当前选中空间。

知识空间用于划分内容边界。一个文档只能归属于一个知识空间；助手可以绑定一个或多个知识空间。

### 3.2 编辑、归档和恢复

选中知识空间后，在右侧标题区域可以：

- 点击 **编辑** 修改名称和说明。
- 点击 **归档** 暂停该空间参与新的检索和摄取任务。
- 对已归档空间点击 **恢复**，重新允许授权用户使用该空间。

归档不会删除历史记录，但已归档空间不会继续为新的问答提供知识。恢复后仍需确认成员权限和助手配置有效。

### 3.3 分配成员

1. 在当前知识空间的成员区域输入用户或群组标识，例如 `group:employees`。
2. 选择角色：读取者、编辑者或管理员。
3. 点击 **分配成员**。

权限变更会作用于后续的列表、问答、引用预览和下载操作。若要撤销权限，需要由管理员通过成员管理接口或相应管理页面移除该成员关系。

### 3.4 上传文档

1. 先选中目标知识空间。
2. 在“文档与摄取”区域点击 **上传文档**。
3. 选择本地文件，页面会自动创建或复用上传数据源。
4. 等待“最近任务”中的任务完成。

默认支持的文件扩展名为：`.pdf`、`.docx`、`.xlsx`、`.pptx`、`.md`、`.txt`、`.html`；默认单文件大小上限为 25 MB。实际限制以当前环境配置为准。

上传后系统会创建文档版本并执行解析、分块、索引等摄取步骤。只有成功处理并成为活动版本的内容，才适合用于问答。

### 3.5 查看文档版本和正文预览

在文档列表中点击 **版本与预览**，可以查看：

- 文档的版本历史；
- 哪个版本是当前活动版本；
- 系统解析出的正文分块、页码和章节信息。

如果预览内容为空、版本不是活动状态或任务仍处于运行中，请先查看最近任务的状态和错误信息。

### 3.6 处理摄取任务

“最近任务”会展示任务状态、当前阶段、尝试次数和脱敏后的错误信息：

- **排队中**：等待处理资源。
- **运行中**：正在解析或建立索引。
- **成功**：文档已完成处理。
- **失败**：本次处理未完成，可点击 **重试**。
- **已取消**：任务被停止。

处于“排队中”或“运行中”的任务可以点击 **取消**。失败任务应先阅读错误信息；格式不支持、文件过大或依赖服务不可用时，重复重试通常不能解决问题。

### 3.7 删除文档

在文档列表中点击 **删除** 会移除该文档的当前可用内容，并触发索引清理。当前页面没有二次确认弹窗，删除前请确认文档名称和所属知识空间。

## 4. 配置：创建并发布助手

### 4.1 创建助手

1. 进入 **配置**。
2. 输入助手名称。
3. 选择一个活动知识空间。
4. 点击 **创建并激活**。

页面会自动创建一个配置草稿，执行校验，并在校验通过后激活首个版本。创建失败时，先确认所选知识空间处于活动状态且当前用户拥有相应权限。

### 4.2 创建新草稿

选中已有助手后，点击 **新建草稿**。草稿会生成一个新的不可变版本快照，用于后续校验和发布。

当前页面以版本生命周期为主，展示提示词、模型、检索、引用、拒答、护栏等组件的版本状态。若需要修改这些组件的具体内容，应通过后端配置接口或对应的配置管理流程完成；页面当前没有逐项编辑这些组件内容的表单。

### 4.3 校验和激活版本

1. 在版本历史中找到目标草稿。
2. 点击 **激活**，系统会再次执行版本校验。
3. 只有没有校验错误的版本才能成为活动版本。

激活后，新的问答使用该版本绑定的知识空间和策略。历史版本会保留，便于审计和回滚；需要回滚时，直接重新激活一个已通过校验的历史版本。

## 5. 问答：查询企业知识

### 5.1 发起问题

1. 进入 **问答**。
2. 在顶部下拉框选择一个活动助手。
3. 在输入框中输入问题。
4. 按 **Enter** 发送；按 **Shift+Enter** 换行。

回答会以流式方式显示。每一轮问答都会重新检查当前用户对知识空间、文档和引用来源的权限。

切换助手会清空当前页面上的对话并开始新会话，不会把原助手的检索证据带入新助手。

### 5.2 阅读引用来源

回答下方的引用按钮会显示来源名称，若有页码则同时显示页码。点击引用后，页面会打开来源抽屉，展示文档标题、页码和解析正文预览。

引用来源可能因权限变化而无法打开；这是预期的安全行为，系统不会为了展示来源而泄露受限内容。

### 5.3 理解回答状态

- **成功**：回答通过验证，并带有可访问的引用。
- **降级**：部分检索依赖不可用，但系统仍使用剩余的授权证据完成回答。
- **拒答/证据不足**：授权范围内没有足够证据，系统不会用未引用的模型知识补全答案。
- **失败**：回答或引用验证未通过，不能把局部生成内容当作可靠答案。

当多个权威来源相互冲突时，回答应列出冲突来源并说明不确定性，不应把任一结论直接当作已确定事实。

### 5.4 提交回答反馈

回答完成后，在底部反馈栏点击 **有帮助** 或 **需改进**。反馈会记录到质量页面，供管理员后续分析。

## 6. 质量：评测助手版本

### 6.1 创建评测集

1. 进入 **质量**。
2. 在 JSON 编辑框中输入题目数组。
3. 点击 **保存新版本**。

每条题目至少应包含问题、预期可回答性、预期来源范围和访问上下文。页面中的示例结构如下：

```json
[
  {
    "id": "sample-1",
    "question": "公司的报销政策是什么？",
    "expected_source_scope": [],
    "expected_answerability": false,
    "access_context": {
      "groups": ["administrators"],
      "roles": ["administrator"]
    }
  }
]
```

保存的是不可变评测集版本；修改题目后应保存新的版本，不要依赖覆盖旧数据。

### 6.2 运行候选版本

点击 **运行当前候选** 后，页面会使用列表中的首个评测集和当前存在活动版本的助手进行评测。运行前确认：

- 至少存在一个评测集；
- 至少有一个助手已激活版本；
- 评测题目的访问上下文与测试目的匹配。

运行列表会展示状态和聚合指标。点击某次运行可以查看详细结果、失败项目和门禁状态。

### 6.3 创建和处理质量门禁

1. 点击 **新增安全门禁** 创建强制门禁。
2. 选择目标运行查看未通过的门禁。
3. 只有确有充分理由时，点击对应的 **覆盖门禁**。
4. 输入覆盖原因；原因会写入审计记录。

覆盖门禁不是修复质量问题的替代方案。发布前应优先修正文档、权限、助手配置或评测数据，再重新运行评测。

## 7. 推荐的完整操作流程

1. 创建并命名知识空间。
2. 为用户或群组分配读取者、编辑者或管理员角色。
3. 上传文档，等待任务成功并检查正文预览。
4. 创建助手并绑定活动知识空间。
5. 校验并激活助手版本。
6. 在问答页提出代表性问题，检查回答和引用。
7. 创建评测集并运行候选版本。
8. 检查质量指标和门禁，确认通过后再投入使用。

## 8. 常见问题排查

### 页面显示“降级”或无法问答

检查 API 是否启动、`/health/ready` 的检查项和配置错误，并确认助手存在活动版本。若使用外部模型、向量库、检索服务或对象存储，还要确认对应依赖已启动且配置正确。

### 上传失败

依次检查文件扩展名、文件大小、当前用户是否具有编辑者或管理员角色、目标知识空间是否活动，以及摄取依赖是否可用。

### 问答返回证据不足

这通常表示当前用户没有访问相关知识空间/文档，或者已授权内容无法达到检索和回答阈值。检查成员关系、文档活动版本、摄取任务状态和助手绑定的知识空间。

### 引用打不开

先确认用户的知识空间权限没有被撤销，文档仍处于活动状态。系统会在打开引用时重新授权，因此权限刚被修改后，旧回答中的引用也可能无法继续访问。

### 助手版本无法激活

先查看版本历史中的校验错误，修复缺失或无效的组件/策略/提供商配置后重新校验。未通过校验的版本不会被设置为活动版本。

### 质量评测按钮不可用

确认已保存评测集，并且至少有一个助手拥有活动版本。若评测运行失败，打开运行详情查看具体失败项目和门禁状态。

## 9. 运维与安全提示

- 不要在文档、日志或截图中暴露 API 密钥、OIDC 凭据、对象存储密钥等敏感信息。
- 生产环境应关闭开发身份认证，启用并正确配置 OIDC；页面显示“系统就绪”不代表业务数据已经完整配置。
- 归档、删除文档、修改成员权限和激活助手版本都会影响后续问答，应在操作前确认租户、知识空间和目标版本。
- 需要追溯问题时，记录页面提示、任务 ID、运行 ID 或回答引用信息，不要复制完整的敏感正文。
